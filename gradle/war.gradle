apply plugin: "war"

bootWar {
    mainClass = "org.springframework.samples.petclinic.PetClinicApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
